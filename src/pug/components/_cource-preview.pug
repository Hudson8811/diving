mixin cource-preview(item)
  div(class= item.cl ? item.cl + ' cource-preview' : 'cource-preview')
    if item.isLabel
      span.cource-preview__label
        | Акция
        +icon('info')(width="20", height="20")
    .cource-preview__image
      img(src="images/cource-preview/" + item.img.n + ".jpg", srcset="images/cource-preview/" + item.img.n + "@2x.jpg 2x", width= item.img.w, height= item.img.h, alt="")
    .cource-preview__content
      h3.cource-preview__title!= item.title
      .cource-preview__list
        each text in item.list
          span= text
      .cource-preview__text!= item.text
      .cource-preview__action 
        +button('Подробнее', 'cource-preview__btn button--white button--more', 'chevron-right', URLS.course)
        .cource-preview__price
          if item.price.old
            .cource-preview__price-old от 55 000₽
          | #{item.price.current}
      a.cource-preview__link(href="#") Приобрести в подарок